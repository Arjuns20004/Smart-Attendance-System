# smart-attendance-system (Working MVP)
Uses MediaPipe Face Detection to detect faces from webcam and log attendance to CSV.
Requirements: mediapipe, opencv-python, flask (for optional viewer)

Run capture:
```bash
pip install -r requirements.txt
python camera_client.py
```
Run viewer:
```bash
python viewer.py
```